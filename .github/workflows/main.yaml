permissions:
  id-token: write
  contents: read

name: CICD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout
        uses: actions/Checkout@v4
      - name: NodeJS Setup
        uses: actions/setup-node@v4
